根据你提供的文件结构，我找到了权限管理相关的核心模块。主要分布在以下几个位置：

# 权限管理相关笔记

## **权限管理相关模块**

### 1. **数据权限控制 (Data Permission)**
- `jeecg-boot-base-core/src/main/java/org/jeecg/common/system/`
  - `query/` - 查询条件生成器，用于动态生成SQL条件
    - `QueryGenerator.java` - 核心查询生成器
    - `QueryRuleEnum.java` - 查询规则枚举
  - `util/`
    - `JeecgDataAutorUtils.java` - 数据权限工具类
    - `SqlConcatUtil.java` - SQL拼接工具
  - `aspect/`
    - `PermissionDataAspect.java` - 数据权限切面
    - `annotation/PermissionData.java` - 数据权限注解
  - `vo/`
    - `SysPermissionDataRuleModel.java` - 权限数据规则模型

### 2. **按钮权限控制 (Button Permission)**
- `jeecg-module-system/jeecg-system-biz/src/main/java/org/jeecg/modules/system/`
  - `controller/SysPermissionController.java` - 权限控制器
  - `entity/`
    - `SysPermission.java` - 权限实体
    - `SysPermissionDataRule.java` - 权限数据规则实体
  - `service/ISysPermissionService.java` - 权限服务接口
  - `service/impl/SysPermissionServiceImpl.java` - 权限服务实现

### 3. **Shiro安全框架**
- `jeecg-boot-base-core/src/main/java/org/jeecg/config/shiro/`
  - `ShiroConfig.java` - Shiro配置
  - `ShiroRealm.java` - 认证和授权核心类
  - `filters/ResourceCheckFilter.java` - 资源权限检查过滤器

### 4. **租户/多租户权限**
- `jeecg-boot-base-core/src/main/java/org/jeecg/config/mybatis/`
  - `MybatisInterceptor.java` - Mybatis拦截器（处理租户ID）
  - `TenantContext.java` - 租户上下文

## 🎯 **针对你需求的实现路径**

### **学生只能查看自己成绩的实现方案：**

#### **1. 数据权限配置**
在 `SysPermissionDataRule` 表中为学生角色配置数据规则：
- **规则字段**: `student_id` (或关联用户ID的字段)
- **规则条件**: `USE_SQL_RULES` 或 `=` 
- **规则值**: `#{sys_user_id}` (系统当前用户ID)

#### **2. 控制器层修改**
修改成绩相关的Controller，例如：
- `jeecg-module-student/src/main/java/org/jeecg/modules/student/controller/StudentGradeController.java`

需要添加数据权限注解：
```java
@PermissionData(pageComponent = "student/StudentGradeList")
@RequestMapping("/studentGrade")
public class StudentGradeController {
    // 你的代码
}
```

#### **3. 查询条件生成**
在Service层使用 `QueryGenerator` 自动注入权限条件：
```java
// 在查询方法中
QueryWrapper<StudentGrade> queryWrapper = QueryGenerator.initQueryWrapper(studentGrade, req.getParameterMap());
// 会自动加入数据权限条件
```

#### **4. 按钮权限控制**
在前端Vue文件中，控制按钮显示：
```vue
<template>
  <!-- 只读模式，隐藏编辑按钮 -->
  <a-button v-if="!readOnly" @click="handleEdit">编辑</a-button>
</template>

<script>
export default {
  computed: {
    readOnly() {
      // 判断是否为学生角色
      return this.$store.getters.userInfo.roles.some(role => role === 'student');
    }
  }
}
</script>
```

## 📁 **关键配置文件位置**

1. **权限表结构**: `db/jeecgai-mysql-5.7.sql` 中的：
   - `sys_permission` - 菜单和按钮权限表
   - `sys_permission_data_rule` - 数据权限规则表
   - `sys_role_permission` - 角色权限关联表

2. **前端权限校验**: 
   - `jeecg-module-demo/src/main/resources/static/` 中的Vue组件
   - 通过 `hasPermission` 方法控制按钮显示

## 💡 **实现步骤建议**

1. **数据权限配置**：
   - 在系统管理 → 菜单管理中找到成绩管理菜单
   - 配置数据权限规则：`student_id = #{sys_user_id}`

2. **角色权限分配**：
   - 给学生角色分配成绩管理的"查看"权限
   - 不分配"编辑"、"删除"等权限

3. **前端控制**：
   - 在成绩列表页面，根据用户角色判断是否显示操作按钮

4. **后端验证**：
   - 在Controller方法中加入`@PermissionData`注解
   - 使用`QueryGenerator`自动过滤数据

# 操作过程
## 后端配置数据权限规则，使学生用户只能查看自己的成绩记录。
### 一、在“成绩管理”表单中配置数据权限规则
1. 登录JeecgBoot系统，进入“系统管理” → “菜单管理”
2. 找到“成绩管理”菜单，点击更多->“数据规则”
3. 添加数据规则：
    - 规则名：只能查看自己
    - 规则条件：自定义sql
    - 规则值：`g.student_no in  ('#{sys_user_code}')`
    - 说明：g.student_no为联表查询的学号字段，sys_user_code为当前登录用户的用户账号（学生用户账号即为学号），写成g.student_no是为了适应在jeecg-boot\jeecg-boot-module\jeecg-module-student\target\classes\org\jeecg\modules\student\mapper\xml\StudentGradeMapper.xml中联表查询的情况。如果简单使用student_no字段会报错字段冲突。
4. 保存数据规则
### 二、给学生角色分配“成绩管理”菜单的查看权限规则
1. 进入“系统管理” → “角色管理”
2. 创建“学生”角色，点击“授权”
3. 授权访问“成绩管理”菜单，点击成绩管理旁边的红色图标，勾选“只能查看自己”数据规则
4. 保存角色授权
### 三、创建学生用户并分配“学生”角色
1. 进入“系统管理” → “用户管理”
2. 创建用户，填写用户信息，用户名即为学生学号
3. 分配“学生”角色，保存用户
### 四、在需要权限控制的Controller方法上添加数据权限注解
1. 打开`jeecg-boot-module-student/src/main/java/org/jeecg/modules/student/controller/StudentGradeController.java`
2. 在需要权限控制的方法上添加`@PermissionData`注解，例如：
```java
@RequiresRoles({"aiadmin","admin"})
```
### 五、使用学生用户登录系统测试
1. 使用刚创建的学生用户登录系统，进入“成绩管理”菜单，只能看到自己学号对应的成绩记录，无法查看其他学生的成绩

## 前端按钮权限控制
### 操作列(TableAction)按钮显示隐藏控制
> 例如：
```javaScript
{
  label: '编辑',
  auth: 'edit',   // 按钮权限标识
  onClick: handleEdit.bind(null, record),
},
```
### 后台进入菜单管理页面配置按钮权限菜单
1. 进入“系统管理” → “菜单管理”
2. 找到“成绩管理”菜单，点击更多->“添加下级”->“按钮”
3. 创建“编辑”按钮，权限标识填写“edit”
4. 保存按钮
### 角色授权按钮权限
1. 进入“系统管理” → “角色管理”
2. 找到“aiadmin”等需要授权的角色，授权新增的“编辑”按钮权限，学生没有授权自然看不到编辑按钮
3. 保存角色授权